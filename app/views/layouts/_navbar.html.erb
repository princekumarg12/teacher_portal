<nav class="navbar" role="navigation" aria-label="Main navigation">
  <div class="navbar-left">
    <span class="brand">Tailswebs</span>
  </div>
  <div class="navbar-right">
    <% if teacher_signed_in? %>
      <span class="navbar-text">Logged in as <%= current_teacher.email %></span>
      <%= link_to 'Home', students_path, class: 'nav-link' %>
      <%= link_to 'Logout', destroy_teacher_session_path, method: :delete, class: 'nav-link' %>
    <% else %>
      <%= link_to 'Login', new_teacher_session_path, class: 'nav-link' %>
    <% end %>
  </div>
</nav>